#include <stdio.h>
/**
 * main - prints the number of arguments
 * @argc: argument count
 * @argv: pointer to arguments passed
 *
 * Return: 0
 */
int main( int argc, char *argv[])
{
	printf("%d\n", argc);
	return (0);
}

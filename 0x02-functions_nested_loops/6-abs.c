#include "main.h"
#include <stdio.h>
/**
* _abs -computes absolute value of integer
* @n: number to be checked for sign
* Return: absolute value of n
*/
int _abs(int n)
{
	if (n < 0)
	{
		n *= -1;
		return (n);
	}
	else
	{
		return (n);
	}
}
